
<p class="text-green-600 text-center font-bold"><%= notice %></p>

<h1 class="text-3xl text-center font-bold">Tasks</h1>
  <div class="flex flex-wrap items-center justify-center" data-controller="hello">
  <p>Filer :&nbsp;</p>
    <%= select_tag(:city_id, options_for_select(Task.status.options, params[:get]), class: 'p-2 item-center flex ', data: {action: 'change->hello#change_filter'}, include_blank: "All")
     %>

  </div>
<div class="bg-white rounded shadow-md p-4">
  <% @tasks.each do |task| %>
    <div class="flex flex-center ">
      
      <%= render task %>
      <p class="text-gray-600 text-sm">
        <%= link_to "Show this task", task, class: "text-blue-600 hover:text-blue-900 font-bold" %>
      </p>
    </div>
  <% end %>
</div>

<%= link_to "New task", new_task_path, class: "bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded" %>
<style>
  #task_status{
    padding: 10px;
    align-content: center;
  }
</style>
<script type="text/javascript">
  // var status = doc

</script>